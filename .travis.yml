language: python
python:
  - 2.7
install:
  - "pip install --allow-external dirspec -r pkg/requirements.pip"
  - "pip install -r pkg/requirements-testing.pip"
script: "python setup.py test"
after_success:
  coveralls

notifications:
  email: false